<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="https://cdn-icons-png.flaticon.com/512/6350/6350271.png" type="image/png">

    <title>Create Blog Post</title>
    <style>
        /* Global Styles */
        body {
            font-family: 'Arial', sans-serif;
            background-image:url('https://visme.co/blog/wp-content/uploads/2017/07/50-Beautiful-and-Minimalist-Presentation-Backgrounds-031.jpg');
            margin: 0;
            padding: 0;
            color: #fff;
        }

        h2 {
            font-size: 2.8rem;
            text-align: center;
            margin: 30px 0;
            color: #fff;
            text-shadow: 0px 4px 8px rgba(0, 0, 0, 0.3);
            animation: fadeIn 1.2s ease-in-out;
        }

        .form-container {
            max-width: 600px;
            margin: 30px auto;
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(12px);
            padding: 40px;
            border-radius: 20px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.3);
            animation: slideIn 1s ease-out;
        }

        .form-container label {
            display: block;
            font-weight: bold;
            margin-bottom: 8px;
            color: #0a0a0a;
            font-size: 1.1rem;
        }

        .form-container input,
        .form-container textarea {
            width: 100%;
            padding: 15px;
            border-radius: 8px;
            border: none;
            background: rgba(255, 255, 255, 0.85);
            margin-bottom: 20px;
            font-size: 1rem;
            color: #333;
            box-sizing: border-box;
            transition: box-shadow 0.3s ease-in-out, transform 0.2s;
        }

        .form-container input:focus,
        .form-container textarea:focus {
            outline: none;
            box-shadow: 0 0 10px rgba(0, 162, 255, 0.7);
            transform: scale(1.01);
        }

        .form-container textarea {
            resize: none;
            height: 150px;
        }

        .form-container button {
            background: rgb(25, 175, 175);
            color: #fff;
            padding: 15px;
            border: none;
            border-radius: 10px;
            cursor: pointer;
            font-size: 1.2rem;
            width: 100%;
            transition: background 0.3s, transform 0.2s;
        }

        .form-container button:hover {
            background: skyblue;
            transform: scale(1.05);
        }

        .form-container button:active {
            transform: scale(1);
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .form-container {
                width: 90%;
                padding: 20px;
            }

            h2 {
                font-size: 2.2rem;
            }

            .form-container button {
                font-size: 1rem;
            }
        }

        /* Animations */
        @keyframes fadeIn {
            0% {
                opacity: 0;
                transform: translateY(-20px);
            }
            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes slideIn {
            0% {
                transform: translateY(50px);
                opacity: 0;
            }
            100% {
                transform: translateY(0);
                opacity: 1;
            }
        }
    </style>
</head>
<body>

    <h2>Create Your Blog</h2>

    <div class="form-container">
        <form action="/upload-blog" method="POST" enctype="multipart/form-data">
            <div>
                <label for="title">Blog Title</label>
                <input type="text" id="title" name="title" placeholder="Enter your blog title" required style="font-family: cursive;">
            </div>
            <div>
                <label for="content">Blog Content</label>
                <textarea id="content" name="content" placeholder="Write your blog content here..." required style="font-family: cursive;"></textarea>
            </div>
            <div>
                <label for="profileImage" >Upload Image</label>
                <input type="file" id="profileImage" name="profileImage" accept="image/*">
            </div>
            <button type="submit">Publish Blog </button>
        </form>
    </div>

    <script>
        // JS for form validation
        document.querySelector('form').addEventListener('submit', function(event) {
            const title = document.getElementById('title').value.trim();
            const content = document.getElementById('content').value.trim();

            if (!title || !content) {
                alert("Both title and content fields are required!");
                event.preventDefault();
            }
        });
    </script>

</body>
</html>
